# Core FastAPI dependencies (minimal)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database dependencies (minimal)
sqlalchemy==2.0.23
pymysql==1.1.0
geoalchemy2==0.14.2

# Data validation (minimal)
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic-core==2.14.1

# HTTP and multipart (minimal)
python-multipart==0.0.6

# Logging (minimal)
structlog==23.2.0
python-json-logger==3.3.0

# Resilience (minimal)
tenacity==8.2.3

# Serverless dependencies (minimal)
mangum==0.17.0
